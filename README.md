# 本地音乐 (LocalMusic)

一款纯离线的 Android 本地音乐播放器，酷狗蓝风格界面，支持手动添加手机本地音乐文件或整个文件夹、歌单管理、沉浸式播放页。

## 功能

- **手动导入**：通过系统文件选择器添加单个/多个音乐文件，或选择整个文件夹批量导入
- **主收藏歌单**：所有导入的音乐自动进入「主收藏」，作为曲库的总入口
- **自建歌单**：新建 / 重命名 / 删除歌单，把曲库中的歌曲添加到任意歌单
- **播放控制**：播放/暂停、进度拖动、上一首/下一首、随机播放、列表循环、单曲循环
- **倍速播放**：0.5x / 0.75x / 1.0x / 1.25x / 1.5x / 2.0x
- **播放队列管理**：查看队列、点选跳播、移除单曲、清空队列、歌曲菜单「下一首播放」
- **单曲容错**：单首歌曲播放失败时自动跳过，不中断整个队列
- **沉浸式播放页**：黑胶唱片风格封面 + 模糊背景 + 播放队列
- **底部迷你播放器**（上一首/播放/下一首 + 旋转封面）+ 系统通知栏控制 + 后台播放
- **歌单管理**：新建 / 重命名 / 删除歌单，把曲库中的歌曲添加到任意歌单
- **深色模式**：跟随系统 / 浅色 / 深色
- 完全离线，无任何网络权限

## 添加音乐的方式

应用采用 **手动添加** 模式（不自动扫描全设备）：

1. 在「本地音乐」页右上角点 **+**
2. 选择 **选择文件**（系统文件选择器，可多选音频文件）或 **选择文件夹**（选择一个目录，递归导入其中的音频）
3. 导入过程会显示进度；完成后歌曲进入「主收藏」并显示封面
4. 已导入过的歌曲会自动去重

> 选中的文件/文件夹会通过 SAF 持久化读取权限，重启后仍可播放。

支持格式：mp3 / flac / wav / m4a / aac / ogg / opus / wma / mid / amr / mka / aiff 等。

## 环境要求

- Android Studio Ladybug (2024.2) 或更新版本
- JDK 17
- Android SDK 35
- 最低支持 Android 8.0 (API 26)

## 在 Android Studio 中打开

1. 启动 Android Studio → **Open** → 选择文件夹 `d:\cursor111\LocalMusic`
2. 等待 Gradle Sync 完成（已配置腾讯云 Gradle 镜像 + 阿里云 Maven 镜像）
3. 连接手机（开启 USB 调试）或启动模拟器
4. 点击 **Run** 安装到设备

> 首次启动会请求 **通知权限**（用于媒体通知栏控制，Android 13+）。添加音乐不需要存储权限（SAF 由用户主动授权）。

## 技术栈

| 模块 | 技术 |
|------|------|
| UI | Jetpack Compose + Material 3（酷狗蓝主题） |
| 架构 | MVVM + ViewModel |
| 播放 | Media3 ExoPlayer 1.5 + MediaSession |
| 存储 | Room 数据库（歌曲持久化 + 歌单关联） |
| 文件选择 | SAF（OpenDocument / OpenDocumentTree） |
| 元数据 | MediaMetadataRetriever（标题/艺术家/专辑/时长 + 嵌入式封面） |
| 封面加载 | Coil |

## 架构要点

- **歌曲持久化**：导入的歌曲存入 Room `songs` 表（`SongEntity`），重启不丢失；歌单通过 `playlist_songs` 关联表引用稳定的 `songs.id`。
- **主收藏 = 曲库**：「主收藏」是虚拟歌单（固定 id = `FAVORITES_PLAYLIST_ID`），其内容即整个 `songs` 表，无需冗余关联。
- **封面**：导入时通过 `MediaMetadataRetriever.embeddedPicture` 提取嵌入式封面，保存到内部存储 `filesDir/albumart/<songId>.jpg`，UI 用 Coil 加载，避免列表滚动时反复解码。

## 注意事项

- 应用完全离线，不包含任何网络权限
- 若导入后无封面，说明该音频文件未内嵌封面图，会显示默认音符占位图
- 若扫描不到音乐，请确认选择了正确的音频文件/文件夹
